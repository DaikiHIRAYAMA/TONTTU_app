<p>outside finish</p>
<p>外気浴中</p>

<%= form_for @timer, url: update5_timer_path(@timer) do |form| %>
  <% if @timer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@timer.errors.count, "error") %> prohibited this timer from being saved:</h2>

      <ul>
        <% @timer.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="actions">
    <%= hidden_field_tag :outside_finish_time, DateTime.now %>
    <%= form.submit "ととのった～"%>
  </div>
  <% end %>